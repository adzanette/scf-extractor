frames:
  database:
    engine: mysql
    host: localhost
    user: root
    password: zanette
    dbName: scf-amazonia
  createTables: false
  clearTables: false
  referenceTable: nilc_scf

reader:
  pageSize: 100

statistics:
  modules: [ 't-score'] #['frequency', 'power-law', 'loglikelihood', 't-score', 't-scoreK']
