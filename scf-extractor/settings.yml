command: Evaluate # [ExtractSCF]

reader:
  module: NILCLexicon # [SentenceDatabaseCorpus, ExampleDatabaseCorpus, PalavrasFileCorpus, NILCLexicon, XmlFile]
  database:
    engine: mysql 
    host: localhost
    user: root
    password: zanette
    dbName: scf-cardiologia
  databaseReader:
    pageSize: 100 
  fileReader:
    path: /home/adriano/src/lexicoNilc.txt

extractor:
  module: NILCLexicon # [SintaticRaspDependency, SemanticFrames, PalavrasDependency, NILCLexicon, Verbnet, Framenet]

builder:
  module: ReferenceFrames # [SCF, SemanticFramesDatabase, ReferenceFrames, NILCLexicon]
  order: position # [position, relevance]
  database:
    engine: mysql
    host: localhost
    user: root
    password: zanette
    dbName: scf-cardiologia
    scfReferenceTable: nilc_frames
  ignoreClasses: ['V', 'SUBJ']
  extractArguments: true
  createTables: false
  clearTables: true

statistics:
  run: false
  modules: ['frequency', 'power-law', 'loglikelihood', 't-score']

filter: 
  #columns: ['verbFrequency', 'frequency', 'relativeFrequency', 'verb.alpha']
  #values: [0, 0, 0, 0.5]
  #operators: ['<', '<', '<', '<']
  columns: ['relativeFrequency']
  values: [0.03]
  operators: ['<']

evaluator:
  scfReferenceTable: framenet_frames
  filter: relativeFrequency
  verbList: ['ser', 'poder', 'ter', 'utilizar']
  minValue: 0
  maxValue: 1
  increment: 0.1
  operator: '<' 
  output: ''