command: ExtractSCF # [ExtractSCF]

reader:
  module: XmlFile # [SentenceDatabaseCorpus, ExampleDatabaseCorpus, PalavrasFileCorpus, NILCLexicon, XmlFile]
  database:
    engine: mysql 
    host: localhost
    user: root
    password: zanette
    dbName: childes
  databaseReader:
    pageSize: 100 
  fileReader:
    path: /home/adriano/src/fndata-1.5/lu/

extractor:
  module: Framenet # [SintaticRaspDependency, SemanticFrames, PalavrasDependency, NILCLexicon, Verbnet, Framenet]

builder:
  module: ReferenceFrames # [SCF, SemanticFramesDatabase, ReferenceFrames, NILCLexicon]
  order: position # [position, relevance]
  database:
    engine: mysql
    host: localhost
    user: root
    password: zanette
    dbName: scf-teste
    scfReferenceTable: framenet_frames
  ignoreClasses: ['AUX', 'SUBJ']
  extractArguments: true
  createTables: false
  clearTables: true

statistics:
  run: true
  modules: ['frequency', 'power-law', 'loglikelihood', 't-score']

filter: 
  columns: ['verbFrequency', 'frequency', 'relativeFrequency', 'verb.alpha']
  values: [0, 0, 0, 0.5]
  operators: ['<', '<', '<', '<']
